# 동시성 제어와 자바 스레드 - 프레젠테이션

> React + reveal.js로 만든 인터랙티브 프레젠테이션

실무에서 마주친 동시성 문제 해결 과정을 담은 CS 스터디 발표 자료입니다.

## 🎯 발표 구성

### Act 1: 문제의 발견 (7분)
- 평범한 시작에서 Race Condition 발견까지
- 실제 버그 상황 재현
- 동시성 문제 시각화

### Act 2: 해결의 과정 (8분)
- 비관적 락 선택 과정
- 코드 구현 및 테스트
- 문제 해결 성공!

### Act 3: 원리의 이해 (15분)
- 프로세스와 스레드
- Java Thread 구현
- 동시성 문제의 본질 (원자성, 가시성)
- 락의 동작 원리
- 스레드 풀과 최적화

### Act 4: 실무의 적용 (8분)
- 실무 적용 패턴 3가지
- 성능 모니터링
- 문제 상황별 대응
- 베스트 프랙티스

## 🚀 빠른 시작

### 1. 의존성 설치

```bash
cd presentation
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

브라우저에서 `http://localhost:5173` 열기

### 3. 프로덕션 빌드

```bash
npm run build
npm run preview
```

## 🎮 프레젠테이션 컨트롤

| 키 | 동작 |
|---|---|
| **→ / Space** | 다음 슬라이드 |
| **← / Shift+Space** | 이전 슬라이드 |
| **↑ / ↓** | 세로 네비게이션 |
| **Home / End** | 처음 / 마지막으로 |
| **f** | 전체화면 |
| **s** | 발표자 노트 (스피커 뷰) |
| **Esc** | 슬라이드 개요 보기 |
| **?** | 도움말 |

## 📁 프로젝트 구조

```
presentation/
├── src/
│   ├── slides/
│   │   ├── TitleSlide.jsx      # 타이틀
│   │   ├── Act1Slides.jsx      # 문제의 발견
│   │   ├── Act2Slides.jsx      # 해결의 과정
│   │   ├── Act3Slides.jsx      # 원리의 이해
│   │   ├── Act4Slides.jsx      # 실무의 적용
│   │   └── ClosingSlides.jsx   # 마무리
│   ├── App.jsx                 # 메인 앱
│   └── main.jsx                # 진입점
├── index.html
├── package.json
└── vite.config.js
```

## 🎨 기술 스택

- **React 18** - UI 컴포넌트
- **reveal.js 5** - 프레젠테이션 프레임워크
- **Vite** - 빠른 개발 서버 & 빌드
- **Prism.js** - 코드 신택스 하이라이팅

## ✨ 특징

### 1. 코드 하이라이팅
Java, SQL 코드가 자동으로 색상 표시됩니다.

### 2. 애니메이션
Fragment 기능으로 단계별 내용 표시가 가능합니다.

### 3. 반응형 디자인
다양한 화면 크기에서 최적화된 레이아웃을 제공합니다.

### 4. 키보드 네비게이션
화살표 키로 자유롭게 이동할 수 있습니다.

### 5. 슬라이드 번호
현재 위치를 항상 확인할 수 있습니다.

## 📝 커스터마이징

### 슬라이드 추가

`src/slides/` 폴더에 새로운 슬라이드 컴포넌트를 만들고 `App.jsx`에 임포트하세요:

```jsx
// NewSlides.jsx
export default function NewSlides() {
  return (
    <section>
      <h2>새로운 섹션</h2>
      <p>내용...</p>
    </section>
  )
}

// App.jsx
import NewSlides from './slides/NewSlides'

// ...
<div className="slides">
  {/* ... */}
  <NewSlides />
</div>
```

### 스타일 변경

`index.html`의 `<style>` 태그에서 CSS 변수를 수정하세요:

```css
:root {
  --r-background-color: #1a1a1a;    /* 배경색 */
  --r-main-color: #f0f0f0;          /* 텍스트색 */
  --r-heading-color: #ffffff;        /* 제목색 */
  --r-link-color: #42b983;           /* 링크색 */
}
```

### reveal.js 설정

`App.jsx`에서 reveal.js 옵션을 변경할 수 있습니다:

```jsx
const deck = new Reveal({
  slideNumber: true,          // 슬라이드 번호 표시
  transition: 'slide',        // 전환 효과
  center: true,               // 수직 중앙 정렬
  width: 1280,                // 슬라이드 너비
  height: 720,                // 슬라이드 높이
  margin: 0.04,               // 여백
})
```

## 🎤 발표 팁

### 발표자 노트 사용

키보드에서 `s`를 눌러 발표자 노트 창을 엽니다. 다음 슬라이드 미리보기와 타이머를 볼 수 있습니다.

### 연습 모드

`Esc` 키를 눌러 슬라이드 개요를 보고 전체 흐름을 파악하세요.

### 시간 관리

- Act 1: 7분
- Act 2: 8분
- Act 3: 15분
- Act 4: 8분
- 마무리: 2분
- **총 40분**

## 🐛 트러블슈팅

### 개발 서버가 시작되지 않음

```bash
# node_modules 삭제 후 재설치
rm -rf node_modules package-lock.json
npm install
```

### 코드 하이라이팅이 작동하지 않음

`index.html`에서 Prism.js CSS가 제대로 로드되었는지 확인하세요.

### 슬라이드가 표시되지 않음

브라우저 콘솔에서 에러를 확인하고, 모든 컴포넌트가 제대로 임포트되었는지 확인하세요.

## 📚 참고 자료

- [reveal.js 공식 문서](https://revealjs.com/)
- [React 공식 문서](https://react.dev/)
- [Vite 가이드](https://vitejs.dev/)

## 📄 라이선스

이 프로젝트는 교육 목적으로 제작되었습니다.

## 🙏 감사의 말

CS 스터디 참여자분들께 감사드립니다!

---

**Good Luck! 🚀**
